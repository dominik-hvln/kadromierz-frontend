# Frontend (Next.js + Capacitor) — uzupełnienie root .gitignore

# Build Next.js / statyczny export dla Capacitor
.next/
out/
build/

# Zależności
node_modules/

# Env
.env
.env.*
!.env.example

# Keystore (podpis APK/AAB)
*.jks
*.keystore

# Debug / cache
*.log
.turbo/
.vercel/

# OS
.DS_Store

# TypeScript (generowany lokalnie — odbuduje się przy `next dev`)
# Odkomentuj poniższą linię, jeśli nie chcesz commitować next-env.d.ts:
# next-env.d.ts
